# Цей файл імпортує логіку (якби вони були в одній папці) або містить код виконання
# Для демонстрації ми припускаємо, що клас TaskManager доступний або імпортований

# Якщо файли в одній директорії, використовуйте: from task_manager import TaskManager

def main():
    # Створення екземпляра менеджера завдань
    # Примітка: Для роботи цього скрипта клас TaskManager має бути визначений або імпортований
    manager = TaskManager() 

    # Використання: додаємо технічні завдання як приклади
    manager.add_task("Налаштувати середовище розробки")
    manager.add_task("Провести рев'ю коду")
    
    # Виведення поточного списку
    manager.show_tasks()
    
    # Позначаємо одне із завдань як завершене
    manager.complete_task("Налаштувати середовище розробки")
    
    # Виведення оновленого списку
    manager.show_tasks()

if __name__ == "__main__":
    # У робочому середовищі важливо, щоб TaskManager був доступний у просторі імен
    if 'TaskManager' not in locals() and 'TaskManager' not in globals():
        print("Помилка: Будь ласка, переконайтеся, що клас TaskManager імпортований з task_manager.py.")
    else:
        main()
